apiVersion: v1
kind: Service
metadata:
  name: jzfs-rust-service
  namespace: gitdata
  labels:
    apptype: jzfs-rust
spec:
  type: ClusterIP
  selector:
    app: jzfs-rust
  ports:
    - protocol: TCP
      port: 34513
      targetPort: 34513
