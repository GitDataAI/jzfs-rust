apiVersion: v1
kind: Service
metadata:
  name: {{.Values.server_name}}
  namespace: gitdata
  labels:
    apptype: {{.Values.name}}
spec:
  type: ClusterIP
  selector:
    app: {{.Values.name}}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
